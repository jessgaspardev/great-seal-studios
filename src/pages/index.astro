---
import MainLayout from "../layouts/MainLayout.astro"
---

<MainLayout>
	<header>
		<div class="hero">
			<!-- Background slides -->
			<div class="hero-background active"></div>
			<div class="hero-background"></div>
			<div class="hero-background"></div>
			<div class="hero-background"></div>
			<!-- Hero Content -->
			<div class="hero-heading">
				<p class="hero-subtitle">Coming Soon</p>
				<h1 class="hero-title">Great Seal Studios</h1>
			</div>
			<!-- Slides Indicators -->
			<div class="slideshow-indicators">
				<div class="indicator active" data-slide="0"></div>
				<div class="indicator" data-slide="1"></div>
				<div class="indicator" data-slide="2"></div>
				<div class="indicator" data-slide="3"></div>
			</div>
		</div>
	</header>
	<main>
		<section class="newsletter">
			<div class="container">
				<div class="newsletter-layout">
					<div class="newsletter-text">
						<p>Join our inner circle to stay updated on our progress! <br/> Worry not! We shall not spam you.</p>
					</div>
					<div class="newsletter-form">
					<!-- to figure out later rip -->
					 <form action="newsletter-sub">
						<input type="email" placeholder="Email" class="input-email">
						<input type="submit" class="submit-button" value="Sign me up">
					 </form>
					</div>
				</div>
			</div>
		</section>
		<section class="manifesto">
			<div class="container">
				<h2 class="title fade-in">Great Seal Manifesto</h2>
				<p class="subtitle fade-in">No great experience without a healthy creating process.</p>
					<p class="text fade-in">This means our creative team works closely together, sharing ideas, working towards the same goal: to produce a world worth living in and stories that stay with players for a very long time. We tend to take our time both for the sake of our games and for the well-being of our team members. We, too, need to have fun during the development stages – otherwise it all falls apart.</p>
				<p class="subtitle fade-in">Knowledge-sharing through visual storytelling.</p>
				<p class="text fade-in">The world of Great Seal was developed as a hobby project of a graduate student testing their knowledge they had gained throughout their years of academia with the sole desire to share them through a single medium: video games. This humble beginning is still very much prevalent. Our goal is to move beyond higher education’s ivory tower and share theories, concepts & ideas, which are often buried under academic jargon. We want to center the untold, the unknown, and a lot of what ifs through conscious storytelling and worldbuilding.</p>
				<p class="subtitle fade-in">Vision not for sale.</p>
				<p class="text fade-in">The greed of higher-ups and incompetence of executives have long been a plague in any industry that relies on the work of artists and creatives. Staying independent is the only way to avoid having our ideas sold out for the highest bidder and getting slaps on our wrists every time we dare to think outside the box. Nothing really blooms in cages. Of course, this decision will highly influence our speed and finances – a reality that we have made peace with and prepared for.</p>
				<p class="subtitle fade-in">Respect for our community.</p>
				<p class="text fade-in">Without them, there is no way to bring our stories to life; they are among the essential cornerstones upon which this studio is built. We understand that every penny matters in this current economy; hence, we will not be asking our fellow gamers to purchase countless expansions in order to receive a full experience. To ensure that people get what they pay for, we aim to create rich tales within fantastical worlds for folks to immerse themselves in, while also staying true to our creative vision. In addition, we wish to build and nurture a healthy community with a strong sense of belonging. This community shall be ever-present: the world of Great Seal does not cease to exist once players hit “Exit Game.”</p>
			</div>
		</section>
	</main>

</MainLayout>

<style>

	/*** HEADER ****/

	.hero {
		height: 100vh;
		position: relative;
		display: flex;
		justify-content: center;
		align-items: center;
		overflow: hidden;
	}

	.hero-background {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background-position: center;
		background-repeat: no-repeat;
		background-attachment: fixed;
		background-size: cover;
		opacity: 0;
		transition: opacity 2s ease-in-out;
	}

	.hero-background.active {
		opacity: 1;
	}

	.hero-background:nth-child(1) {
		background-image: url(/public/images/tovalandslide.avif);
	}

	.hero-background:nth-child(2) {
		background-image: url(/public/images/erdomslide.avif);
	}

	.hero-background:nth-child(3) {
		background-image: url(/public/images/murvaslide.avif);
	}

	.hero-background:nth-child(4) {
		background-image: url(/public/images/erdomBslide.avif);
	}

	.hero-background:nth-child(2)::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.4); /* 30% dark overlay */
    z-index: 1;
}

	.hero-background:nth-child(4)::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.4); /* 30% dark overlay */
    z-index: 1;
}

	.slideshow-indicators {
		position: absolute;
		bottom: 2.5rem;
		left: 50%;
		transform: translateX(-50%);
		display: flex;
		gap: 1rem;
		z-index: 10;
	}

	.indicator {
		width: 1rem;
		height: 1rem;
		border-radius: 50%;
		background-color: rgba(247, 225, 169, 0.5);
		cursor: pointer;
		transition: background-color 0.3s ease;
	}

	.indicator.active {
		background-color: #f7e1a9;
	}

	.hero-heading {
		text-align: center;
		position: relative;
		z-index: 10;
	}

	.hero-title {
		font-size: 5rem;
		color: #f7e1a9;
		letter-spacing: 10px;
		font-weight: 500;
		text-shadow: 3px 3px 3px rgba(0,0,0,0.7);
		animation: 3s fadeInUp;
	}

	.hero-subtitle {
		font-size: 2rem;
		text-transform: uppercase;
		letter-spacing: 7px;
		text-shadow: 2px 2px 2px rgba(0,0,0,0.7);
		animation: 3s fadeInDown;
	}

	/*** CONTENT SECTION ****/

	.title, .subtitle, .text {
		animation: fade linear;
		animation-timeline: view();
		animation-range: entry 0 cover 40%;
	}

	.title {
		text-align: center;
		padding-bottom: 6rem;
	}

	.subtitle {
		color: #F3ECDB;
		font-size: 1.6rem;
		font-weight: 400;
		letter-spacing: 3px;
		text-transform: uppercase;
		padding-bottom: 5rem;
	}

	.text {
		line-height: 170%;
	}

	.text:not(:last-child) {
		padding-bottom: 7rem;
	}

	/*** NEWSLETTER SECTION ****/

	.newsletter {
		background-color: #161616;
	}

	.newsletter-layout {
		display: flex;
		flex-direction: row;
		gap: 2rem;
	}

	.newsletter-text {
		flex: 1;
	}

	.newsletter-form {
		flex: 1;
	}

	.input-email {
		width: 20rem;
		height: 2.5rem;
		padding: 1rem;
		margin-right: .5rem;
	}

	.submit-button {
		height: 2.5rem;
		padding: 0 1.7rem;
		background-color: #f7e1a9;
		color: #111;
		font-family: Minion Pro Regular, sans-serif;
		font-size: 1rem;
		font-weight: 600;
		text-transform: uppercase;
		border-style: none;
		cursor: pointer;
	}


</style>

<script>

  // Slideshow variables
	let slides;
  let indicators;
  let currentSlide = 0;
  let slideInterval = null;
  
  // start the automatic slideshow
  const startSlideshow = () => {
      slideInterval = setInterval(function() {
          nextSlide();
      }, 5000);
  }
  
  // pause the slideshow
  const pauseSlideshow = () => {
      if (slideInterval) {
          clearInterval(slideInterval);
      }
  }
  
  // reset the interval (restart after manual navigation)
  const resetInterval = () => {
      pauseSlideshow();
      startSlideshow();
  }
  
  // go to next slide
  const nextSlide = () => {
      currentSlide = (currentSlide + 1) % slides.length;
      updateSlide();
  }
  
  //  go to specific slide
  const goToSlide = (slideIndex) => {
      currentSlide = slideIndex;
      updateSlide();
  }
  
  // update slide visibility and indicators
  const updateSlide = () => {
      // Update slide visibility
      slides.forEach(function(slide, index) {
          if (index === currentSlide) {
              slide.classList.add('active');
          } else {
              slide.classList.remove('active');
          }
      });
      
      // Update indicators
      indicators.forEach(function(indicator, index) {
          if (index === currentSlide) {
              indicator.classList.add('active');
          } else {
              indicator.classList.remove('active');
          }
      });
  }
  
  // Initialize slideshow when DOM is loaded
  document.addEventListener('DOMContentLoaded', function() {
      // Get elements after DOM is loaded
      slides = document.querySelectorAll('.hero-background');
      indicators = document.querySelectorAll('.indicator');
      
      // Start automatic slideshow
      startSlideshow();
      
      // Add click handlers for indicators
      indicators.forEach(function(indicator, index) {
          indicator.addEventListener('click', function() {
              goToSlide(index);
              resetInterval();
          });
      });
      
  });
</script>
